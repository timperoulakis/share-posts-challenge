<div class="card mt-4">
    <div class="card-body">
      <p><%= comment.content %></p>
      <div class="card-footer text-muted"><%= "posted by #{comment.user.nickname} - #{time_ago_in_words(comment.created_at)} ago" %></div>
    </div>
</div>
